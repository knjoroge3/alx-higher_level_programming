#!/usr/bin/python3

"""
this code returns True if the
if the object is exactly an instance of the
specified class ; otherwise False
"""

def is_same_class(obj, a_class):
    """
    function Prototype defination
    """
    if type(obj) == a_class:
        return True
